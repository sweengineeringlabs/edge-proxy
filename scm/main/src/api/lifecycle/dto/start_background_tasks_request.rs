//! [`StartBackgroundTasksRequest`] — request for [`LifecycleMonitor::start_background_tasks`](crate::api::lifecycle::traits::LifecycleMonitor::start_background_tasks).

/// Request to start any long-running background tasks.
pub struct StartBackgroundTasksRequest;
