//! [`HealthRequest`] — request for [`LifecycleMonitor::health`](crate::api::lifecycle::traits::LifecycleMonitor::health).

/// Request to aggregate health across all handlers and subsystems.
pub struct HealthRequest;
