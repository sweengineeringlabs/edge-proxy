//! [`ShutdownRequest`] — request for [`LifecycleMonitor::shutdown`](crate::api::lifecycle::traits::LifecycleMonitor::shutdown).

/// Request to gracefully shut down a Controller instance.
pub struct ShutdownRequest;
