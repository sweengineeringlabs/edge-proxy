//! `ProxySvc` — static access facade handle for proxy factory operations.

/// Handle type for proxy factory and validation operations.
///
/// All methods are associated functions — callers use `ProxySvc::method_name()`
/// rather than constructing an instance.
pub struct ProxySvc;
