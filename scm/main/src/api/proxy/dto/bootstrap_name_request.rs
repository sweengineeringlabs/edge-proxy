//! [`BootstrapNameRequest`] — request for [`ProxyComposer::bootstrap_name`](crate::api::proxy::traits::ProxyComposer::bootstrap_name).

/// Request to query a composer implementation's stable identifier.
pub struct BootstrapNameRequest;
