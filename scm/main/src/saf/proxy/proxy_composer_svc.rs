//! ProxyComposer SAF facade — identity constant for the composition concern.

/// Identifies the ProxyComposer concern within the proxy crate.
pub const PROXY_COMPOSER_CONCERN: &str = "proxy_composer";
