//! SAF factory identity for [`NoopValidator`](crate::api::NoopValidator).

/// Service identity for the `NoopValidator` factory contract.
pub const NOOP_VALIDATOR_SVC_FACTORY: &str = "noop_validator_factory";
