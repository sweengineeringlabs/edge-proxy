//! SAF factory identity for [`NullLifecycleMonitor`](crate::api::NullLifecycleMonitor).

/// Service identity for the `NullLifecycleMonitor` factory contract.
pub const NULL_LIFECYCLE_MONITOR_SVC_FACTORY: &str = "null_lifecycle_monitor_factory";
