//! SPI extension point for the `proxy` domain.
//!
//! No external-lib-backed `ProxyComposer` variant exists yet — every
//! implementation in this crate lives in `core/proxy/`. This module marks the
//! extension point for a future external-lib-backed strategy without
//! providing one prematurely.
