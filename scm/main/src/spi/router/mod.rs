//! SPI extension point for the `router` domain.
//!
//! No external-lib-backed `Router` variant exists yet — every implementation
//! in this crate lives in `core/router/`. This module marks the extension
//! point for a future external-lib-backed strategy without providing one
//! prematurely.
