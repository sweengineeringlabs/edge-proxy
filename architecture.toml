[project]
name = "edge-proxy"
layout = "single"

[whitelist]
allow = ["Cargo.toml", "Cargo.lock", "CHANGELOG.md", "architecture.toml", "config", "scripts", "src", "tests", "examples", "target", "bootstrap.sh", "bootstrap.ps1", "struct.toml", "docs", "README.md", "LICENSE"]
